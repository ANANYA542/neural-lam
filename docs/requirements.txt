# ──────────────────────────────────────────────────────────────────
# Neural-LAM Jupyter Book Documentation — Pinned Dependencies
# ──────────────────────────────────────────────────────────────────
# Tested on Python 3.11 macOS (Apple Silicon + Intel)
#
# KEY CONFLICT RESOLUTION:
#   jupyter-book <1.0 pins sphinx <6 and breaks with sphinx-autoapi >=3
#   jupyter-book >=1.0 supports sphinx 5/6/7, resolving the conflict
#
# SOLUTION: Use the modern stack:
#   jupyter-book 1.0.3  (supports Sphinx 7.x)
#   sphinx-autoapi 3.0.0 (requires Sphinx >=6.1)
#   Sphinx 7.2.6
# ──────────────────────────────────────────────────────────────────

# Core engine — Sphinx 7.2.6 is the latest 7.2.x, compatible with all below
Sphinx==7.2.6

# Jupyter Book 1.x supports Sphinx 5, 6, 7
jupyter-book==1.0.3

# Auto-generated API docs — 3.0.0 requires Sphinx >=6.1
sphinx-autoapi==3.0.0

# Jupyter Notebook support in MyST
myst-nb==1.1.2

# Mermaid diagram rendering
sphinxcontrib-mermaid==0.9.2

# Copy button on code blocks
sphinx-copybutton==0.5.2

# docutils 0.20.1 avoids AssertionError bugs in 0.16–0.19
docutils==0.20.1

# ── Sub-dependency pins (avoid resolver surprises) ───────────────
sphinx-book-theme==1.1.3
sphinx-external-toc==1.0.1
sphinx-design==0.5.0
sphinx-jupyterbook-latex==1.0.0
myst-parser==2.0.0
sphinxcontrib-bibtex==2.6.2
